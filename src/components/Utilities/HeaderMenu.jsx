const HeaderMenu = ({ title }) => {
  return (
    <div>
      <div className="p-8">
        <h2 className="text-center tex-2xl text-color-primary font-bold">{title}</h2>
      </div>
    </div>
  );
};

export default HeaderMenu;
